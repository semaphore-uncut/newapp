version: v1.0
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Build"
    task:
      env_vars:
        - name: APP_ENV
          value: prod
      jobs:
      - name: Docker build
        commands:
          - checkout
          - cache restore gems-$SEMAPHORE_BRANCH_NAME-$(checksum Gemfile.lock),gems-$SEMAPHORE_BRANCH_NAME,gems-master-
          - bundle install --deployment --path vendor/bundle -j 4
          - cache store gems-$SEMAPHORE_BRANCH_NAME-$(checksum Gemfile.lock) vendor/bundle
          - sem-service start postgres
          - bundle exec rspec
