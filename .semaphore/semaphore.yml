version: v1.0
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: Tests
    task:
      jobs:
      - name: RSpec
        commands:
          - checkout
          - bundle install --deployment -j 4 --path vendor/bundle
          - bundle exec rspec
